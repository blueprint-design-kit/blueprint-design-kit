import { VariantPickerClient, type VariantPickerProps } from './VariantPickerClient.js';

export default function VariantPicker(props: VariantPickerProps) {
    return <VariantPickerClient {...props} />;
}
