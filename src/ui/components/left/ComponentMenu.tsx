import { ComponentMenuClient, type ComponentMenuProps } from './ComponentMenuClient.js';

export default function ComponentMenu(props: ComponentMenuProps) {
    return <ComponentMenuClient {...props} />;
}
